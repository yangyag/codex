server {
    listen 8080;
    server_name _;

    # /admin으로 접근 시 슬래시 추가
    location = /admin {
        return 302 /admin/;
    }

    location /admin/ {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /admin/index.html;
    }
}
